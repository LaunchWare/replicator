import blocks, { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";

import { useConfirmationModal } from "./useConfirmationModal";
import "../css/confirmationModal.css"

export const DefaultComponent = () => {
  const { showModal, modal } = useConfirmationModal({promptText: "You sure?", action: () => { console.log("confirmed")}}) ;
  return (
    <>
      <button type="button" onClick={() => showModal() }>Do Something</button>
      {modal}
    </>
  )
}

<Meta title="useConfirmationModal" />

# Default Confirmation

<Canvas>
  <Story name="default">
    <DefaultComponent />
  </Story>
</Canvas>

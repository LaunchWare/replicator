import blocks, { Meta, Story, Canvas } from "@storybook/addon-docs";

# Modals

## Default Modal

<Canvas>
  <Story id="modal--default-modal" />
</Canvas>

## Routed Modal

You can use a `RoutedModal` to integrate React routes with modal displays.

<Canvas>
  <Story id="modal--routed-modal" />
</Canvas>
